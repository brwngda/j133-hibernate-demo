package pl.sda.hibernate.demo;

public class TryCatchPrzykladFinally {
    public static void main(String[] args) {
//        // wywołaj try
//        Session session = null;
//        try {
//            session = HibernateUtil.INSTANCE.getSessionFactory().openSession();
//
//            // jeśli sukces (brak błędu), to nie wywoła się catch
//        } catch (IOException ioe) {
//            // jeśli złapiemy błąd, to wywoła się catch
//
//        } finally {
//            // niezależnie od ścieżki wykonania finally musi wykonać się zawsze!
//            if (session != null) {
//                session.close();
//            }
//        }
    }
}